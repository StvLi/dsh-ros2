{
  "name": "dsh-ros2-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm workspace for the dsh-ros2 plugin family (7 cordis bundles: common/core/profile/moveit/safety/vision/aggregate)",
  "scripts": {
    "typecheck": "pnpm --filter dsh-ros2-common run build && pnpm -r typecheck",
    "test": "pnpm --filter dsh-ros2-common run build && pnpm -r test",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  }
}
